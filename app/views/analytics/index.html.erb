<div class="max-w-3xl mx-auto p-6 space-y-6">
  <h1 class="text-2xl font-bold text-blue-700 dark:text-white">📊 Productivity Analytics</h1>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-center">
    <div class="bg-white dark:bg-gray-800 p-4 rounded shadow">
      <p class="text-sm text-gray-500 dark:text-gray-400">Today</p>
      <p class="text-xl font-semibold text-blue-600"><%= distance_of_time_in_words(@today.seconds.ago, Time.current) %></p>
    </div>

    <div class="bg-white dark:bg-gray-800 p-4 rounded shadow">
      <p class="text-sm text-gray-500 dark:text-gray-400">This Week</p>
      <p class="text-xl font-semibold text-blue-600"><%= distance_of_time_in_words(@this_week.seconds.ago, Time.current) %></p>
    </div>

    <div class="bg-white dark:bg-gray-800 p-4 rounded shadow">
      <p class="text-sm text-gray-500 dark:text-gray-400">This Month</p>
      <p class="text-xl font-semibold text-blue-600"><%= distance_of_time_in_words(@this_month.seconds.ago, Time.current) %></p>
    </div>
  </div>

  <div>
    <h2 class="text-lg font-semibold text-gray-700 dark:text-gray-200 mt-6 mb-2">Focus Time (Last 7 Days)</h2>
    <ul class="space-y-1 text-sm text-gray-700 dark:text-gray-300">
      <% @daily_totals.sort.each do |date, duration| %>
        <li>
          <strong><%= date.strftime("%A") %></strong> —
          <%= distance_of_time_in_words(duration.seconds.ago, Time.current) %>
        </li>
      <% end %>
    </ul>
  </div>
</div>